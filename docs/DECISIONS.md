# DECISIONS

One line per judgment call: date, decision, why.

## 2026-09-16 - art, voice pass, and ship

- 2026-09-16 | Generated 27 business blog images and 10 candid Live Band images (gpt_image_2_5, 43 credits). | Every post on the site now has card art; the LWS blog had 27 placeholder tiles. Business art matches the existing conceptual still-life style (navy/slate/marble, one cyan accent). Jon: "make some of the live band blog photos iphone style... casual, candid, like a quick take", so ten posts whose SUBJECT is a phone or social media got deliberately imperfect snapshots: tilted horizons, high-ISO grain, blown highlights, direct flash. The other twenty keep the cinematic set, so the blog reads as a mix rather than a format change.
- 2026-09-16 | Candid images went in as NEW files (`<slug>-phone.jpg`) with an explicit `image:` in frontmatter, rather than overwriting the .webp originals. | bandImage() checks `image:` before the disk lookup, so the swap is two frontmatter lines and reverting needs no regeneration. sips on this machine cannot write webp, which is the other reason these are .jpg.
- 2026-09-16 | `.blog-thumb--empty` was missing `aspect-ratio: 16/9` and collapsed to the height of its own text (39px). | Pre-existing, but only 2 posts hit it before the archive was published and 31 did after. Fixed to match `.blog-thumb` exactly; both measure 355x200.
- 2026-09-16 | All 27 published archive posts rewritten per the jonvoice skill. | Jon: "yes...all jon voice. thats a must." The 2022-12 batch in particular was spun text ("Any small business worth its salt in this day and age"). Verified after: 0 em dashes, 0 banned phrases, 0 posts over one exclamation mark, no paragraph over 90 words, 293-758 words each.
- 2026-09-16 | The jonvoice skill says to use em dashes freely. The global CLAUDE.md bans them in blog copy as a HARD STOP. The ban won. | The skill predates the ban, and CLAUDE.md says the rule is about reputation, not grammar. Used ellipses and sentence breaks instead. Worth reconciling in the skill itself at some point.
- 2026-09-16 | One kept "Submit" in web-design-tips, deliberately. | It appears as `"Call me" beats "Submit" every day of the week`, which is Jon arguing against the word, not using it as a CTA. The banned-phrase check flags it; it should stay.
- 2026-09-16 | HTML comments are now stripped from the built output by a build hook in astro.config.mjs (1127 removed). | 103 em dashes were shipping inside comments across 27 pages, several of them build notes narrating edits. None were visible copy so no rule was broken, but developer commentary has no reason to reach a client's browser. Conditional comments and anything inside script/style are left alone.
- 2026-09-16 | Deleted .github/workflows/scheduled-publish.yml outright. | Jon: "delete that....dead issue. gone." With the Live Band series fully published, 0 posts are future-dated, so the drip has no job. Without NETLIFY_BUILD_HOOK (off the table) it also failed every day at 11:30 UTC and would have emailed Jon each time.
- 2026-09-16 | Corrected specs/qa-checklist.md line 22. | It claimed the badge is natively 168x56 and must never be a fixed 150x37. The asset has been 150x37 since the 2026-08-27 swap, so the checklist would have caused a future "fix" that broke a correct implementation. Also recorded the opacity:1 requirement, since the PNG has no alpha.
- 2026-09-16 | Pushed to the livewebstudios-v3 repo, NOT the live site. | Jon: "the current (old) site is on a livewebstudios repo. this new site is on livewebstudios-v3... i will keep it like that and just switch the domain via netlify domain settings." The two repos are separate Netlify sites; cutting over is a domain change Jon makes himself, not a push.

## 2026-09-16 - publishing the WordPress archive (42 drafts triaged)

- 2026-09-16 | Deleted 15 of the 42, published 27. | Jon: "look at the content. make this work... i dont want to get red flagged." Six were under 260 words (one was 69, a promo blurb). Two were raw ChatGPT output presented as articles, which is a bad look for an agency selling AI in 2026. Three carried a stale year in the title or premise. Four competed head-to-head with a stronger live post, which splits the ranking signal so neither wins.
- 2026-09-16 | Kept the original historical dates rather than re-stamping the archive as new. | Jon: "definitely date back those older content articles." A 2015 post dated 2015 is an asset for a firm claiming since 2004; the same post dated this week is a lie Google can check against its own crawl history.
- 2026-09-16 | Spread the 2022-12-26/28/29 cluster across Dec 2022 to Oct 2023, and split the 2015-07-08 pair. | Eighteen posts appeared on two days, eleven on one, all sharing a title pattern ("The Benefits of X", "The Importance of Y"). That is the bulk-publish signature Jon was worried about. Dates were chosen two to four weeks apart and ordered so neighbouring posts are on different subjects. Every post now owns its own day (the one remaining same-day pair is one business post and one musician post, which never share an index).
- 2026-09-16 | Re-dating changed NO urls. | `slugOf()` strips the YYYY-MM-DD- filename prefix, so the slug is whatever follows it. Renaming the prefix moves the date and leaves the URL untouched. Verified: 0 broken internal refs across 152 pages.
- 2026-09-16 | "New for 2025: Custom Web Apps..." was retitled, not deleted, unlike the other stale-year posts. | Its premise is not stale: it describes a service Jon still sells. Only the year in the title had aged, so the year came off and his wording stayed. Deleting it would have removed a live-service page.
- 2026-09-16 | Fixed a typo in one title: "Websites Services101: Using an \"All-in-One\" company" -> "Website Services 101: Using an All-in-One Company". | Also cleared stray escaped quotes left by the WordPress export. A typo fix, not a rewrite: Jon's copy rule protects his writing, not an export artifact.
- 2026-09-16 | One post had its 316-character intro PARAGRAPH wrapped in `###` by the WordPress export, which read as an h1 -> h3 skip. Unwrapped to plain text. | Scanned every post for the same pattern (any heading over 120 characters); this was the only one.
- 2026-09-16 | `.blog-thumb--empty` never had `aspect-ratio: 16/9`, so a card with no hero art collapsed to the height of its own text (39px) and read as a broken strip. | Pre-existing, but only 2 posts hit it before; 31 do now. Restored to the same 16/9 box as `.blog-thumb`. Verified both render at exactly 355x200.
- 2026-09-16 | The placeholder tile now names the post's CATEGORY instead of showing "LWS". | 27 archive posts carry no hero art. Naming the category makes the tile informative and deliberate rather than a missing image, and matches the typographic category placeholder the Live Band blog already uses. No new art needed, which is what made it shippable tonight. The colour rule had to go in mosa-skin.css: its `.blog-thumb--empty span` rule lands after global.css and was overriding it.
- 2026-09-16 | FIXED, was my own regression: converting `ai-isnt-coming-its-here.md` off WordPress frontmatter left no newline after the closing `---`, fusing the body to the frontmatter. The post built and titled correctly but rendered an EMPTY article. | The grep that "confirmed" the body was present had matched the meta description, not the body. Now guarded: every build is checked for empty or thin post bodies (all 82 pass).

## 2026-09-15 - launch audit fixes (titles, meta, headings, images, headers)

- 2026-09-15 | The brand suffix on blog <title> tags is now conditional (`withBrand` in src/lib/seoTitle.ts): appended only when the whole title fits in 60 characters, dropped otherwise. | 71 of 97 post titles were over 60 WITH the suffix but only 29 were over on their own. The suffix costs 21 characters on the LWS blog and 24 on Live Band, so it, not the headlines, was the cause on 42 posts. The brand is already in the domain shown above the title, so losing it beats truncating the headline that explains the page.
- 2026-09-15 | New optional `seoTitle:` frontmatter field, set on the 29 posts whose headline is over 60 by itself. | Jon wrote every headline and wants them kept ("i wrote everything....all mine"). This lets the <title> be short for the search result while the H1 on the page stays the headline as written. Verified on three posts: title 48-55 chars, H1 unchanged.
- 2026-09-15 | Image width/height are READ FROM THE FILE at build time (src/lib/imageSize.ts), not hand-typed. | 139 images were missing them. Hand-typing 139 pairs is 139 chances to be wrong, and a wrong pair is worse than none: it reserves the wrong box, so the shift still happens AND the image renders distorted. The parser was checked against macOS `sips` on all 309 images in public/images: 309 matches, 0 mismatches.
- 2026-09-15 | PhotoBg reads its own dimensions rather than taking them from the caller. | The props existed already and nine of ten callers simply omitted them, which is how eight bluestage heroes shipped with no aspect ratio. A component that reads the file cannot be forgotten.
- 2026-09-15 | The Footer carries its own `<h2 class="visually-hidden">Site navigation</h2>`. | The 2026-09-14 pass moved the footer columns h4 -> h3, which fixed the 72 pages whose last body heading is an h2 but left every THIN page broken (thank-you, newsletter, forms: no body h2 at all, so h1 -> h3). Anchoring the level inside the footer fixes it for any page, including ones not written yet. Cleared 4 of the 15 skips on its own.
- 2026-09-15 | The card-grid pages got `visually-hidden` h2s rather than new visible section headings. | 10 pages needed a heading between the h1 and the card h3s. A visible one means writing new copy onto seven service pages without Jon seeing it. Hidden is a structural fix, not a copy change. On /services the label also CANNOT be the visible pill: `.sec-head h2` in global.css is more specific than `.eyebrow--tag`, so the pill would have blown up to a 3rem title. Verified the pill still computes to 12.8px.
- 2026-09-15 | contact.astro's "Talk to Jon." went h3 -> h2 and its three style selectors widened to `:is(h2,h3)`. | It is the only heading between that page's h1 and the footer, so it was a genuine skip. Widening the selectors rather than switching them means the rules cannot silently stop applying, and a future h3 under it still gets the treatment. `:is()` takes its most specific argument, so specificity is unchanged.
- 2026-09-15 | verifiedsecured.png renders at opacity 1. | Jon: "the verifiedsecured.png is dark". Two rules dimmed it, `.badge img{opacity:.6}` in mosa-skin.css winning over `.85` in global.css. The file has NO alpha channel, so any opacity under 1 blends the dark footer through the white plate and greys the whole badge.
- 2026-09-15 | HSTS ships WITHOUT `includeSubDomains` and without `preload`. | The apex and www are on Netlify and always HTTPS, but a subdomain on another host (webmail, cPanel, staging) would become unreachable in every browser that had seen the header, curable only by waiting out the max-age. `preload` is a one-way door needing a removal request. Both are one-line additions once every subdomain is confirmed HTTPS.
- 2026-09-15 | No Content-Security-Policy in netlify.toml. | The site loads Google Analytics, Mailchimp, Formspree and Netlify Identity. A CSP written blind would break a form or the CMS login silently. That one wants its own pass with the network tab open.
- 2026-09-15 | `rel=preload` is emitted only for PHOTO heroes (46 pages), never for the video or shader heroes. | There is no single image to prioritise on a VideoBg page, and preloading video bytes competes with the text for bandwidth. The preload href is built from the same relPrefix() call as PhotoBg's src so the two cannot drift: a href differing by one "../" downloads the hero twice. Verified 0 mismatches across 46 pages, and one network request for the hero on a live page load.
- 2026-09-15 | The lightbox <img> on /services/custom-styles-and-carousel-cards keeps NO width/height. | It is the only image left without them. Its src is set by JS from the full-size href, which is a different file from the thumbnail, so any dimensions would be a guess. It is also a user-opened overlay, and CLS excludes shifts within 500ms of user input, so it contributes nothing to the metric. Wrong numbers would be worse than none.
- 2026-09-15 | Four blog slugs renamed, not redirected. | `3-23-pm-google-changed-...` leaked a WordPress export timestamp; three others ran 68-87 characters. Checked first that no rule in public/_redirects targets a specific post (they all land on /blog) and that nothing in src or public links to them, so there is nothing to redirect from. v3 has not shipped, so these URLs have never been live.
- 2026-09-15 | NOT DONE, needs Jon: the 42 restored WordPress posts are all `draft: true` and render nowhere. 67 business posts exist, 25 build. Flagged rather than flipped: publishing 42 articles is an editorial call.
- 2026-09-15 | NOT DONE, needs Jon: specs/qa-checklist.md line 22 says the footer badge is "native 168x56" and must never be a fixed 150x37. The asset on disk IS 150x37 (verified with sips) since the 2026-08-27 swap, so the checklist is stale and the current markup is correct.

## 2026-09-14 — Namesake live preview generator

- 2026-09-14 | Committed the pending Session 5/6 working tree as its own commit (677f721) before starting, rather than stashing or building on a dirty tree. | The handoff's first stop condition is a dirty `git status`. Jon answered "your choice". Committing is non-destructive, preserves the work, and keeps the Namesake diff isolated.
- 2026-09-14 | Chip chrome and modal accent use the existing global `--brass` (#C9A55A) token. | The handoff says "the --brass rule the sub-nav already uses for is-active". That rule is `.subnav--namesake { --sub-accent: var(--brass) }` with `box-shadow: inset 0 -2px 0`. Reused verbatim, no new colors. Note the page's own `.theme-namesake` block separately overrides `--cyan` to a near-identical brass, so the two agree on screen.
- 2026-09-14 | All ten woff2 faces downloaded cleanly from google-webfonts-helper, latin subset. No fallbacks needed. | Sources and OFL licensing recorded in public/fonts/namesake/LICENSES.txt.

## 2026-09-14 - side requests during the Namesake run

- 2026-09-14 | Curtain reveal on the home page holds 1s after `.in` before the panels travel, via a new `--curtain-hold` token on `.curtain-stage`. | Jon: "delay this, its open before i scroll down, just a few seconds", then "split the delay, its too slow, cut it in half". The shared `.reveal` observer fires when the stage's TOP edge crosses 92% of the viewport, so the 1.5s open finished before the section was looked at. Held as a token rather than retuning the observer, which every other `.reveal` on the site depends on. Measured 1121ms from `.in` to panel travel.
- 2026-09-14 | Live Band card CTAs moved from `.btn-ghost` to `.btn-primary` and restate `background: var(--cyan)` / `color: var(--on-accent)`. | Jon: "make these buttons yellow branded color background with dark text (reverse)". Both colors have to be restated because mosa-skin.css re-skins `.btn-primary` site-wide to flat white and lands after global.css, so the `.theme-live-band` amber never reached the button on its own. Values still come from the theme's own tokens, so no fourth yellow enters the palette. Contrast 12.73:1.
- 2026-09-14 | Added `white-space: normal` to `.card-cta`. | `.btn` is `white-space: nowrap`, which pushed the longest label 9px past its card at 1280. Pre-existing, invisible on a ghost button, obvious under a solid fill.

## 2026-09-14 - Namesake Session 1

- 2026-09-14 | Island root is `display: contents` so one mount point supplies two grid items (`.nsp-controls` under the headline on the left, `.nsp-stage` spanning both rows on the right). | The handoff wants the card and the preview in different hero columns, which would otherwise need two islands and a shared store for one piece of state.
- 2026-09-14 | The hero grid places those two with DESCENDANT selectors, not child selectors. | `display: contents` removes `.nsp-root`'s box from layout but leaves it in the DOM tree, so it is still the direct child and `>` matches nothing. Written with `>` the rules silently no-op and all three items auto-place into the wrong cells. Cost one debugging round; noted here so it is not reintroduced.
- 2026-09-14 | Name font size is four bands per style on `data-nsp-len`, measured from the LONGEST RENDERED LINE rather than the whole string. | bold and warm set a name on two lines, so "Maximilian Featherstonehaugh III" must size off "Featherstonehaugh III" (21) and not 32. Verified across 4 styles x 4 names: zero text overflow.
- 2026-09-14 | The bold marquee derives its repeat count from name length instead of using a fixed one. | A fixed 8 repeats cannot do both jobs: it covers only 620 of the 1440 for "Al" (row reads half empty) and builds 5292px of clipped layout for a 32-character name. Now 3 to 19 repeats, every name covering the full width.
- 2026-09-14 | `.nsp-page` sets its own `text-align` and the four page bodies are `height: 100%`. | The live frame is a `<button>`, which the UA stylesheet centres, and the body elements were auto-height so their `flex: 1` middles had no free space to absorb. Both had to be fixed for the three renders (frame, modal, export host) to agree, which Session 2 depends on.
- 2026-09-14 | Scale is written as `--nsp-scale` through `element.style.setProperty`, the island's only style attribute. | Container width over 1440 is a runtime number that cannot live in a stylesheet. Passing it as a custom property keeps the CSS owning what it is used for. Frame height is reserved by `aspect-ratio: 16/10` rather than a measured pixel height, so nothing shifts before the fonts resolve.
- 2026-09-14 | Chip swatches and per-chip display faces are CSS rules keyed off `data-style`, not inline custom properties. | Keeps the no-inline-styles rule intact with 12 swatches and 4 type samples.

## 2026-09-14 - Namesake Session 2

- 2026-09-14 | The ten faces are registered TWICE: through the FontFace API (rendering, URL from `base`) and through a runtime `<style>` element carrying the same `@font-face` rules. | Session 2D of the handoff predicted this and it is real, not precautionary. html-to-image builds its embedded font CSS by walking `document.styleSheets`; FontFace-API faces are not in there. Measured on the bold style: the name's ink is 854px wide in the PNG with the style tag and 940px without it, against 868px on screen. Without the tag the PNG ships a fallback face. Same URLs, so the browser serves both from one fetch.
- 2026-09-14 | `.nsp-modal` restates `margin: auto`. | A `<dialog>` centres itself with the UA's `margin: auto`, and global.css line 57 resets every element to `margin: 0`, which took that with it and pinned the modal to the top left corner.
- 2026-09-14 | Modal width is `min(92vw, calc((92vh - 96px) * 1.6))`, not `calc(92vh * 1.6)`. | The 16:10 frame is not the whole dialog. The button row and the note sit under it, so the width that makes the FRAME fit 92vh pushes Save PNG under the fold on a short window.
- 2026-09-14 | Escape is closed by an explicit `keydown` handler on the dialog rather than relying on the browser's native `cancel`. | Verified in this environment that the Escape keydown reaches the document while the dialog's `cancel` never fires, so the native path alone left the modal undismissable from the keyboard. Closing an already closed dialog is a no-op, so this is safe alongside the native behaviour.
- 2026-09-14 | The export host renders only while the modal is open. | It is a full unscaled 1440x900 subtree; there is no reason to carry it in the DOM for every visitor who never opens the modal.
- 2026-09-14 | The four reference PNGs ARE on disk, in the session scratchpad under namesake-pngs/. | The Browser pane's sandbox discards downloads (confirmed by a full home-directory scan: no file anywhere), so the page posted each blob to a throwaway Node receiver on 127.0.0.1:4399 instead. Dimensions re-verified on disk with sips, independent of the browser: all four exactly 2880x1800, 194 to 1592 KB. The classical export was opened and read back: genuine Libre Caslon Display and Lora italic, not a fallback.
- 2026-09-14 | NOT DONE, needs a decision: the four QA skill passes (/polish, /colorize, /layout, /animate) from Session 3 have not been run. They exist in this environment. Held back because they rewrite design decisions and Jon was iterating live on other pages during this run.

## 2026-09-22 - Expert Witness build + six side fixes

- 2026-09-22 | The expert witness page carries NO social proof of any kind: no testimonials, no logo strip, no stats band, no case counts, no "trusted by". | Jon has no prior testimony history, so any of those would be a fabricated claim. The page sells practitioner qualification and method instead. A claim-scan grep is documented in the session handoff and a comment block at the top of the page file restates the rule for whoever edits it next. If the page ever looks like it needs more credibility, the answer is more specificity about method, not more claims.
- 2026-09-22 | OG card named `og-expert-witness.jpg`, not the spec's `images/og/expert-witness.jpg`. | `Base.astro`'s `ogIsCard` regex only matches `images/og-*.jpg`. Following the spec path literally would have silently dropped the `og:image:width`/`height` tags with no error. Do not rename it.
- 2026-09-22 | LEGAL is its own 4th services nav column, not a 4th link under BRAND. | The expert witness practice sells to attorneys, not to the small businesses the other twelve links serve. Filed under BRAND it reads as a design service.
- 2026-09-22 | Services mega panel widened 720 -> 900px and anchored to `header.nav` via `studios--svc{position:static}` in global.css. | Four columns wrap "AI Business Services" and "Custom Styles & Cards" below 840px. But `.mega` is centred on its trigger, so every extra 100px of panel hangs another 50px off the LEFT of the viewport; it ran 63px off-screen at 1100. Anchoring to the header is the same trick `.studios--dir` already uses. The rule must live in global.css, not Nav.astro's scoped block, because `.studios{position:relative}` is declared in global.css and wins on source order. That fix then opened a 13px dead strip under the trigger, closed with `margin-top:-29px; padding-top:45px` on `.mega--services` for an 8px hover overlap.
- 2026-09-22 | Hero uses a still `PhotoBg` with scrim `hero`, not `VideoBg` with `strong`. | Every other service page runs video; legal buyers read rather than watch. Scrim lightened so the hologram reads, since the copy sits in its own glass panel anyway.
- 2026-09-22 | FAQ accordion markup, styles and script duplicated from `faq.astro` rather than extracted to a shared component. | That page keeps its pattern page-scoped, and extracting it would have meant refactoring a working page. Heading level differs (h3 here, h2 there). Keep the two in sync.
- 2026-09-22 | Band SEO card opacity overridden scoped to `.lbs-card`, not on the global `.glass-inner`. | `.glass-inner` at `rgba(11,18,32,.5)` is correct for the dark main site; only the amber band stage wash needed `rgba(9,14,26,.9)`. Shared component, so this also fixed band home, forms, personal-sites and portfolio.
- 2026-09-22 | FAQ card opacity raised to `.88`, and `.faq-item.is-open` now LAYERS its cyan wash over the dark panel instead of replacing it. | Cards were `rgba(7,11,20,.52)` over the bright `faq-mov.mp4`. The real bug was the open state: it swapped the dark ground for a 5% cyan wash, so an opened answer sat on bare video.
- 2026-09-22 | RESOLVED (was open): the site says "23 years" everywhere, including the expert witness page. | The page shipped as "Twenty-Two Years of the Work Itself" while `about.astro`, `seo.astro`, `Nav.astro`, `404.astro` and three blog posts all said 23. Jon's call: 23 everywhere. Only the expert witness heading changed; the other files were already correct.
- 2026-09-22 | The closing CTA on the expert witness page carries the `jon-webdesk.mp4` clip (`cta cta--video`), even though the page's own rule bans a hero video. | Jon, mid-session, pointing at the Live Band pricing payment note: "put this animated background behind the section." The NO HERO VIDEO rule is about the hero only. Same recipe as `/faq` and the band pricing note: the clip fills the CARD, not the section, so the copy holds to the scrimmed left column and Jon sits right of frame. Scrim, mobile crop (`object-position:70% center`) and `min-height:0` below 860px all came free from the existing `.cta--video` rules; zero new CSS.

## 2026-09-22 - /services/email-writing build

- 2026-09-22 | Logged here, in `docs/DECISIONS.md`, not a new `DECISIONS.md` at the repo root. | The handoff prompt said "DECISIONS.md at the repo root" but the file the project has kept since 2026-09-14 is this one. A second file at the root would have split the log in two.
- 2026-09-22 | THE PROBLEM section uses a page-scoped `.em-prose` measure helper. | It is three paragraphs with no list and no art, and there is no standalone prose class to reach for: `.copy` only gets its type treatment inside `.split`. Same precedent as `.ew-card` on expert-witness.astro, which added page-scoped helpers rather than new global section classes.
- 2026-09-22 | The de-emphasised "before" text uses `--muted-2`, not `--muted`. | The prompt offered `.card-points` as a fallback treatment, but no such class exists (only `.copy-points`), so a real token was the better answer. First pass used `--muted` on the theory that `--muted-2` failed contrast, which was wrong: that math came off global.css's `:root` hex values, and **mosa-skin.css redefines all four ink tokens as white-alpha** (`--ink` .85, `--muted` .74, `--muted-2` .58). Measured on the rendered card ground, which composites to rgb(3,3,3): `--muted-2` is 6.9:1 and `--ink` is 14.6:1. The hierarchy reads and the dim half still clears WCAG AA for body text. Anyone tuning colour on a card must measure against the skin, not `:root`.
- 2026-09-22 | The six voices are unnumbered `<article>` cards, not an `<ol>`. | They are a set, not a sequence. The deck numbers them 1 to 6 only to enumerate. HOW IT RUNS is the section that is genuinely ordered, and that one is an `<ol class="grid-svc steps">` matching expert-witness.
- 2026-09-22 | The before/after examples drop the deck's enclosing quotation marks. | Every word is verbatim. The blocks carry BEFORE and AFTER labels and a coloured left rule, so the framing already says "this is an email" without straight quotes, which render cheaply at body size.
- 2026-09-22 | On the four FIT cards the bold lead-in becomes the h3 and the remainder becomes the body. | Items 1, 3 and 4 are a single sentence split across the two, so the body opens on "And" rather than restating the subject. Preserves every word of the deck at the cost of three sentences starting on a conjunction, which is inside Jon's register anyway.
- 2026-09-22 | THE PROBLEM section takes "THE PROBLEM" as its eyebrow. | The deck gives an explicit eyebrow for every other section and none for this one. Its own section name was the only candidate that did not invent copy.
- 2026-09-22 | The hero uses a plain `.hero-btns` pair, not expert-witness's `hero-foot hero-foot--split` with the link row. | Those three classes live in HeroActions.astro's SCOPED style block and do not reach a page, which is why expert-witness carries ~60 lines duplicating them. This page has no third-party link row to justify paying that again.
- 2026-09-22 | No `ogImage` and no `preloadHero` prop. | The hero is a VideoBg, so there is no single still to prioritise, and Base's default `images/og-default.jpg` still matches the `og-[a-z-]+\.jpg` test that emits the og:image dimension tags. Same as every other VideoBg service page.
- 2026-09-22 | Step 2C needed no CSS change. | `.svc-col` is a plain flex column with no four-link assumption, and BUILD already carried five. Verified with the panel open at 1280 (four columns, all 153px, panel 190 to 1090 inside 1280, no label wrapping) and at 375 (single column, panel 20 to 365 inside 375, no page overflow).
- 2026-09-22 | The footer Services column is now 9 links, the longest of the five. | Per the prompt: added without rebalancing the others.
- 2026-09-22 | Kill-list "click here" and "submit" hits on the built page are pre-existing sitewide chrome and were left alone. | "click here" is `CrookedSign.astro` line 28, rendering in the Footer on all 149 pages; it is Jon's own uncommitted work, already flagged for his decision in the expert-witness handoff, and rewriting his deliberate design copy was not this task's scope. "submit" is `type="submit"` on the newsletter button, whose visible label is "Subscribe": a required HTML attribute, not visible text. Both counts are identical on untouched pages. Zero hits originate from this page.
- 2026-09-22 | The two em dashes in the built page are `Base.astro`'s reveal-script JS comments (lines 192 and 241), present on all 149 pages. | The ban excludes code. The one em dash this build introduced was in the new page's own header comment and was replaced with a colon, because this brief said "no em dashes anywhere" rather than the global carve-out for code.
- 2026-09-22 | The commit does NOT use `git add -A`, and carries more than its subject line says. | The prompt assumed a clean tree. It was not clean: the whole uncommitted expert-witness build was sitting in it, plus three files Jon had staged himself before any of this (the CrookedSign feature), plus this session's edits to the two docs. `Nav.astro`, `services.astro` and `Footer.astro` each carry expert-witness changes AND email-writing changes in the same file, so the two cannot be separated at file granularity. Committing everything under "Add /services/email-writing page" alone would have mislabelled it, so the subject line is the prompt's and the body lists what actually went in. Jon's three staged CrookedSign files were held back. Nothing pushed.
- 2026-09-22 | The six before/after examples were cut to roughly half their length. | Jon: "they are too complicated. make them simpler, quicker to read and understand." The deck's afters ran three and four sentences, which defeats a card you are meant to scan. Every after is now one or two short sentences, same content, same voice. This overrides the handoff's "copy verbatim" rule, which was Jon's to override.
- 2026-09-22 | The four build steps got card art, and the row is 4-up rather than the base `.grid-svc` 3-up. | Jon: "put images in these cards, make the images literal, not techy, make them look like a real email, gmail, outlook." Five generated images total (4 steps + 1 CTA), gpt_image_2_5, 3:2, 900x604, 328K for the set. Every prompt asked for interface text as "soft indistinct grey bars, no readable lettering", which is what keeps garbled pseudo-text off the cards while the UI still reads as an inbox. Four steps in a 3-up grid would have stranded one card alone on a second row, so `.em-steps` runs 4 / 2 / 1.
- 2026-09-22 | This page departs from the site's wireframe-hologram art style on purpose. | Every other service page runs the cyan hologram look (the `industry-law-firm.jpg` family). Jon asked for literal here, and he is right: the product IS email, so a photographed dark-mode inbox sells it where an abstract hologram would not.
- 2026-09-22 | The CTA uses `cta cta--art` with an image, not `cta--video`. | Jon: "put an image here." `.cta--art` is the existing two-column CTA pattern from /services/ai-platform and the ten industries pages; the other call sites pass an SVG component, this one passes an `<img>`, which the .cta-art column takes without any new layout CSS.
- 2026-09-22 | The ambient background is `bg-blue-fog.mp4` behind the SIX VOICES section only, not the whole page. | Jon: "an appropriate non-annoying animated background, not too techy." bg-blue-fog is a slow gradient haze with no imagery in it at all, which rules out the techy options (bg-color-tv is TV static, line-to-graph is a chart). Scoped to one section, and six opaque glass cards cover most of it, so the drift only reads in the gutters. Motion behind body copy is what makes these annoying.
- 2026-09-22 | Six voice glyphs live in the page, not in `src/lib/navIcons.ts`. | Jon asked for a voice icon per card with a reference image of a head in profile emitting sound waves. Drawn to navIcons' house rules (24x24, stroke only, 1.6 weight, round caps, nothing finer than ~2px) so they read as the same family, but navIcons.ts is the nav dropdown's set and nothing else consumes these. Shared head, per-voice emission: two waves, wave + stop bar, straight arrow, forward arrow, wave + plus, three waves. Two first drafts were scrapped after rendering them at 100px: a second head for The Handoff and a circular repeat arrow for The Second Ask both collapsed into a blob at 30px.
- 2026-09-22 | THE PROBLEM section is now a lead line, a three-item `.copy-points` list, and two short paragraphs, down from three paragraphs and about 150 words to 97. | Jon: "this doesn't look good. use bullet mark, shorter too. content is good." The three concrete examples were buried mid-paragraph and are the strongest thing in the section, so they became the bullets.
- 2026-09-22 | Verification moved off the dev server to a static server over `dist/`. | The dev server on 4323 belongs to another session and was serving stale CSS: none of the new scoped rules were in its stylesheet while all of them were correct in `dist/`. This is Gotcha #1 from the expert-witness handoff, hit for the third time. The `dist-preview` entry already in `.claude/launch.json` serves the built output on 8901 and is the reliable way to check layout.
- 2026-09-22 | The "Worth It If You Recognize Yourself Here" fit section was cut, along with its `fit` array. | Jon: "i dont understand that section. why is that there?" Fair question, because it was not doing work. It was a self-qualification block restating THE PROBLEM, and once THE PROBLEM became a bullet list the page carried two bulleted lists making the same point a screen apart. Its strongest item, "You have an unsent folder", is already the opening line of THE PROBLEM. Page now runs hero, problem, six voices, four steps, FAQ, CTA.
- 2026-09-22 | The six voice glyphs, and the FAQ envelope drift, are `--brass`. | Jon: "icons should be another color. you decide", then after an AI-violet pass, "no, try gold icon, the gold thats on some other pages". Brass is the right one of the three golds here because THIS PAGE ALREADY RUNS IT: `.step-num` on the four build cards is `var(--brass)`, so gold reads as a second deliberate accent down the page rather than a one-off on the voice cards. `--amber` was out because it means Live Band Web Studios site-wide, `--cat-gold` because it means a blog category. The drift was moved off cyan to match, so the page carries ONE warm accent rather than two unrelated ones, and so the FAQ band separates from the cool bg-blue-fog section above it. Either way the icons had to leave cyan: `--cyan-soft` was already carrying the job line, the AFTER rule, the BEFORE/AFTER labels, the bullet markers and the whole FAQ accordion, so a ninth cyan element read as more of the same instead of as the mark on the card. The card TITLES went brass too (Jon: "try the titles of the voices also gold"), so the header row of each card is one mark instead of a gold glyph bolted onto a white heading; the scoped `.em-head h3` is (0,2,1) and beats mosa-skin's `.card h3` at (0,1,1) without !important. Brass measures 8.9:1 on the rendered card ground, against the 3:1 WCAG 1.4.11 asks of a graphic and of 20px+ text.
- 2026-09-22 | The FAQ band got eight drifting outline envelopes, CSS only, no JS and no new asset. | Jon: "a subtle animated background for the black backgrounded section, maybe a floating email icon, not tacky, something smart, somewhat techie, not cheezy." `#faq` was the only bare `sec-pad` on the page: no is-alt, no has-scan, no clip. What keeps a floating-icon field off the cheesy side is restraint on four axes: opacity (5% at first, raised to 13% on 2026-09-22 after Jon could not see it at all: brass at 5% over the page ground composites to about rgb(15,15,15) against rgb(5,7,11), a ten-step difference out of 255, which is invisible rather than subtle; 13% lands near rgb(30,27,21), and when Jon asked a THIRD time whether it was rendering it went to 22%, about rgb(48,42,31). It had been rendering correctly and was visible in his screenshot each time, but a decorative layer you have to squint at and ask about is not subtle, it is wasted; the copy sits at 0.74 to 1.0 white, so 22% is still nowhere near competing with it. Stroke went 1.4 to 1.7 at the same time, because at 24 to 72px the thin outline was hiding them as much as the opacity was), sizes from 24 to 72px so it reads as depth rather than a repeated sprite, eight durations from 61 to 107 seconds that share no common multiple so the field never visibly resynchronises, and placement confined to the side gutters clear of the 780px reading column. Animates transform and opacity only, honours prefers-reduced-motion, and drops the two largest below 900px where the gutters close up. Same glyph as the `mail` icon added to navIcons.ts this session.
- 2026-09-22 | The envelope field is weighted into the gutters, not guaranteed clear of the FAQ column, and the comment says so. | First pass claimed every envelope was "parked in the side gutters clear of the 780px reading column". Measuring it at 1440 showed 2 of 8 crossing the column, so the claim was wrong. Two positions were moved (77 to 81 percent, 71 to 84 percent) which gets it to 0 of 8 at 1440, and the comment now states the real mechanism: positions are percentages against a fixed max-width column, so which ones clear it moves with viewport, and the accordion panels are opaque enough to cover any that drift behind them.
- 2026-09-22 | Voice card header is title hard left, glyph hard right. | Jon 2026-09-22. `justify-content: space-between`, and the h3 moved AHEAD of the svg in the markup rather than being pushed over with `order`, so reading order matches visual order and the heading still comes first for a screen reader.
- 2026-09-22 | The envelope drift runs behind BOTH flat-black bands, THE PROBLEM and the FAQ, not just the FAQ. | Jon: "i dont see the background." Two causes. He was on localhost:4323, the stale dev server, which had none of it. And it was only on the FAQ while he was looking at THE PROBLEM. Those two are the page's only sections with no art and no wash of their own, so both needed it. `#problem` already gets position, overflow and a lifted `.wrap` from `.has-scan`, so it needed no new positioning rules.
- 2026-09-22 | Meta pass on both new service pages. | Jon: "fix all meta stuff." email-writing's description ran 172 chars against Google's ~155-160 truncation, led on "the emails you dread" and "bad news" (the framing Jon had already pulled out of the hero), and named sections the page no longer has. Rewritten to 140, mirroring the hero's three jobs. expert-witness ran 190, so its "plaintiff or defense" tail never showed; trimmed to 152 with the "website accessibility expert witness" phrase left intact at the front because that is the one worth ranking for. Audited all 14 service pages: the rest sit 115 to 153, and `custom-styles-and-carousel-cards` is fine at 143 (it just declares its description as `const DESC`, which a naive grep misses).
- 2026-09-22 | email-writing got its own OG card, `og-email-writing.jpg`, replacing the sitewide `og-default.jpg`. | Named to match `Base.astro`'s `ogIsCard` regex `/\/images\/og-[a-z-]+\.jpg$/`, without which the og:image:width and height tags silently drop. Same trap the expert-witness build hit. Two attempts to generate a bespoke wide card failed at Higgsfield's end (credits refunded automatically), so rather than spend a third, it was cropped from the 1024x688 original of the sent-folder image still in the session scratchpad: crop to 1024x538 for the 1.905 ratio, then up to 1200x630, a 1.17x scale rather than the 1.33x that cropping the shipped 900px version would have needed.

## 2026-09-22 - Receiver CTAs across the band pages, and a .cta h2 leading bug

- 2026-09-22 | The four form cards on `/live-band-web-studios/forms` now use `ReceiverCta` instead of plain `.btn` links. | Jon: "put the ctas on this page the same as these meter buttons." Each carries its own `peak` (44 / 22 / 38 / 16) because four identical meters in a row read as a repeated graphic rather than four instruments; the two `primary` cards swing highest.
- 2026-09-22 | `ReceiverCta` gained a button mode, and the Live Band home page's seven CTAs moved onto it. | Jon: "same with the buttons on this page." Six of those seven are `More Info` dialog triggers, which are `<button data-modal-open>` rather than links, so the component could not be dropped in as-is. It now takes an optional `modal` prop and renders through a dynamic `Tag` (`button` when `modal` is set, `a` otherwise) rather than duplicating forty lines of inner markup, so the meter can never drift between the two forms. UA button styles (border, padding, font, colour, text-align, appearance) are reset on `.rcta` so the two are pixel-identical. Verified the dialog still opens and closes on click: the binder is the attribute selector `[data-modal-open]` and prefixes `modal-`, so dropping the `.btn` class changed nothing.
- 2026-09-22 | `.cta h2` never declared a line-height, so it inherited the BODY leading of 1.6 while every other display heading on the site runs 1.12. Fixed in global.css. | Jon: "spacing is off on the tell me... line." Measured on the email-writing CTA: 48px type on a 76.8px line, against `.sec-head h2` at 1.12. It has been wrong for as long as the rule has existed and nobody saw it, because a CTA heading that fits on one line shows no symptom; the two-line one on this page pulled 46px of dead air between the lines. This is a design-system fix, not a page fix, so it lands in global.css next to the `.sec-head h2` rule it now matches. Checked single-line CTAs on /services and /about afterwards: ratio 1.12, still inside the panel, no page overflow.
